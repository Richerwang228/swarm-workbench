# Web console

This package is the Next.js presentation layer for Swarm Workbench. It starts
tasks through the local FastAPI service and reduces SSE events into observable
agent, todo, chat, and activity state.

Use the repository-level `./start.sh --demo` for the supported development path.
See the root [README](../../README.md) and
[architecture](../../docs/ARCHITECTURE.md).
